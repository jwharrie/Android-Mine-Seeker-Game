<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="a276.cmpt.mineseeker">

    <application
        android:allowBackup="true"
        android:icon="@drawable/jet"
        android:label="@string/game_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".WelcomeActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".GameScreenActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".HelpScreenActivity"
            android:screenOrientation="landscape" />
        <activity android:name=".OptionScreenActivity"
            android:screenOrientation="landscape" />
    </application>

</manifest>